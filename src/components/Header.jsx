/**
 * HEADER COMPONENT
 *
 * Placeholder component for the app header. Currently renders nothing (null)
 * but is kept in the component structure for future use (e.g., title, instructions, settings).
 */

export default function Header() {
  return null;
}
